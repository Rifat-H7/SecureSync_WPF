<ui:FluentWindow x:Class="SecureSync.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:SecureSync"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <ui:TitleBar Title="Secure Sync"/>
        </StackPanel> 
        <StackPanel Grid.Row="1" Margin="10">
            <Button Content="Select File" Width="100" Click="SelectFileButton_Click" />
            <TextBlock Name="SelectedFileText" Margin="10,10,10,10" Text="No file selected" />

            <ProgressBar Name="UploadProgressBar" Height="20" Minimum="0" Maximum="100" />

            <TextBlock Name="UploadStatusText" Margin="10,10,10,10" Text="Upload status will be displayed here" />

            <Button Content="Download File" Width="100" Click="DownloadFileButton_Click" />
            <TextBlock Name="DownloadStatusText" Margin="10,10,10,10" Text="Download status will be displayed here" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
